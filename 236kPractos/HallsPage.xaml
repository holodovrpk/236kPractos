<Page x:Class="_236kPractos.HallsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:_236kPractos"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HallsPage">

    <StackPanel>
        <TextBlock Text="Залы" FontSize="24" />

        <ScrollViewer Height="350">
            <ItemsControl x:Name="GroupHall" Width="600" >
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Background="Aquamarine" Margin="5">
                            <TextBlock Text="{Binding Name}" FontSize="30" FontWeight="ExtraBold"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Rows, StringFormat=Количество рядов: {0}}" FontSize="20" Width="400"/>
                                <TextBlock Text="{Binding Places, StringFormat=Количество мест: {0}}" FontSize="20"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Button Content="🗑️" Margin="5" Click="Del_Click"/>
                                <Button Content="✏️" Margin="5" Click="Edit_Click"/>
                            </StackPanel>
                        </StackPanel>

                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Content="➕ Добавить новый зал" Margin="5" Click="Add_Click"/>
    </StackPanel>
</Page>
